apiVersion: v1
kind: Service
metadata:
  name: movie-service
spec:
  selector:
    app: movie-service
  ports:
    - protocol: TCP
      port: {{ .Values.movieService.service.port }}
      targetPort: 8000
  type: {{ .Values.movieService.service.type }}